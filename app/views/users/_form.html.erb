<% method = user.persisted? ? "PATCH" : "POST" %>
<% btn_msg = user.persisted? ? "Update #{user.username}" : "Create Account" %>


<%= form_for(user, url: user_url) do |f| %>
<input  type="hidden" name="_method" value="<%= method %>">
<%= f.label :username %>
<%= f.text_field :username, placeholder: user.username,
class: 'form-control' %>

<%= f.label :password %>
<%= f.password_field :password, class: 'form-control' %>

<br>
<%= f.submit btn_msg, class: "btn btn-primary" %>
<% end %>
