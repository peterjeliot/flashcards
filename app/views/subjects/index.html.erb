<table>
  <% @subjects.each do |subject| %>
  <tr>
    <td>
      <%= link_to subject.name, user_subject_url(subject) %>
    </td>
  </tr>
  <% end %>
</table>

<br>

<%#= button_to "Create a New Subject", new_user_subject_url,
method: :get, class: 'btn btn-primary' %>
